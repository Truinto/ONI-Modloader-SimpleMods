using Harmony;
using UnityEngine;
using System.Reflection;
using System.Collections.Generic;
using System.Reflection.Emit;
using System.Linq;
using System;

namespace CustomizeBuildings
{
    public class DLC1
    {
        //public static void OnXXXLoad()
        //{
        //    var harmony = HarmonyInstance.Create("CustomizeBuildings.Fumihiko");
        //    harmony.Patch(Type.GetType("SludgePressConfig, Assembly-CSharp").GetMethod("ConfigureBuildingTemplate"),
        //        postfix: new HarmonyMethod(typeof(SludgePressConfig_ConfigureBuildingTemplate).GetMethod("Postfix")));
        //    AccessTools.Field(Type.GetType("TUNING.EQUIPMENT.SUITS, Assembly-CSharp"), "ATMOSUIT_DECAY").SetValue(null, 0f);
        //    AccessTools.Field(Type.GetType("TUNING.EQUIPMENT.SUITS, Assembly-CSharp"), "ATMOSUIT_ATHLETICS").SetValue(null, -2f);
        //    AccessTools.Field(Type.GetType("TUNING.EQUIPMENT.SUITS, Assembly-CSharp"), "OXYGEN_MASK_DECAY").SetValue(null, 0f);
        //}
    }
}